# build outputs (images are large — not tracked; rebuild from scripts/)
*.img
*.iso
*.qcow2
*.raw
*.raw.xz
# release artifacts + the resolved per-kver config (regenerated by 04/05; never committed)
vend/
config-6.*
# python / tooling cruft
__pycache__/
*.pyc
.DS_Store
*.swp
vend*/
