# Release location for scripts/flash.sh.
#
# The URL is GATED until public launch (#12) and is intentionally left empty here — committing it would
# publish the download location before the leadership gate. Internal validators: take the URL from your
# validation invite and either pass it to flash.sh as arg 2, or set it below in your local checkout
# (do not commit the value). At public launch, set it here in one commit.
RELEASE_BASE_URL=
